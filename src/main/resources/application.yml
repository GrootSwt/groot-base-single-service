server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.106.49.0:3306/single_blog?serveTimezone=GMT
    username: root
    password: 123456
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  redis:
    host: 39.106.49.0
    password: swt620523
  application:
    name: single-blog
blog:
  # 日志定时传输时间
  transferTime: "0 0 2 1 * *"
  # redis中用户token过期时间（min）
  expireTime: 30
  # 文件根目录
  rootPath: E://files